// StyledButton.js
import React from 'react';
import './Button.css';

const StyledButton = () => {
  return (
    <button className="styled-button">
      Start
    </button>
  );
};

export default StyledButton;
